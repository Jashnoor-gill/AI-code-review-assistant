[project]
name = "ai-code-review-assistant"
version = "0.1.0"
description = "Unified AI-powered code review assistant scaffold"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
